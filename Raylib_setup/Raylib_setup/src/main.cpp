#include <raylib.h>

int main()
{
    InitWindow(600, 500, " ");

    SetTargetFPS(60);

    while (!WindowShouldClose())
    {
        BeginDrawing();

        

        EndDrawing();
    }

    CloseWindow();
    return 0;
}